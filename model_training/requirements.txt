# PyTorch (Python 3.13 compatible)
# For GPU, install from the CUDA wheel index that matches your driver support.
# For Python 3.13, cu128 currently has full matching wheels for torch/torchvision/torchaudio.
# Example: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.6.0
torchvision
torchaudio

# Hugging Face Transformers
transformers==5.3.0
datasets==4.6.1
evaluate==0.4.6
accelerate==1.13.0
huggingface-hub==1.6.0

# Optional: GPU monitoring and utilities
gputil==1.4.0

# Data & ML utilities
pandas>=2.0.3
numpy>=1.22.0
scikit-learn>=1.3.2
pyarrow>=11.0.0
matplotlib
seaborn

# Misc
pyyaml>=6.0
tqdm
pytest>=8.0.0
tldextract==5.3.1